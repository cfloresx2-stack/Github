[package]
name = "calc_engine_wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
calc_engine = { path = "../calc-engine" }
compliance_engine = { path = "../compliance-engine" }
